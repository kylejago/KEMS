{
  "schema": 1,
  "bundle": "__RELEASE_VERSION__",
  "channel": "alpha",
  "components": {
    "kems_core": {
      "version": "__RELEASE_VERSION__",
      "required": true,
      "delivery": "home-assistant-update",
      "restart": "home_assistant"
    },
    "dashboard": {
      "version": "__RELEASE_VERSION__",
      "required": true,
      "delivery": "kems_core"
    },
    "panel": {
      "version": "0.9.0-alpha9-panel.0",
      "required": false,
      "delivery": "kems_core"
    },
    "property_web": {
      "version": "0.9.0-alpha9-web.0",
      "required": true,
      "delivery": "kems_pi"
    },
    "pi_agent": {
      "version": "0.9.0-alpha9-web.0",
      "required": true,
      "delivery": "kems_pi"
    },
    "pi_system": {
      "version": null,
      "required": false,
      "delivery": "kems_pi"
    },
    "public_web": {
      "version": "0.9.0-alpha9-public.0",
      "required": false,
      "delivery": "ionos-sftp"
    }
  },
  "maintenance": {
    "required": true,
    "reason": "Alpha9 coordinated parity baseline; Alpha9.21 KEMS-only future-flow reserve-policy correction after live plan evidence showed the customer-facing pre-cheap projection could publish Grid IMPORT around 15% SOC because agile_flow_presentation reused the 15% planning/export reserve as the battery-to-home floor. Preserve 15% as the normal optimisation target and deliberate-export floor, but let ordinary house service continue below 15% toward the independent 10% absolute safety floor; only at the 10% floor may expensive grid serve residual house demand, and retain the existing 12% recovery latch once hard safety has activated. Apply this only to the future plan-flow projection and its SOC presentation, retaining the existing rolling optimiser allocation and current-routing safety owner; add executable production-projection regressions for 14.5% house bridging without day-rate import, export stopping before house service, the 10% grid handover, and the 12% recovery latch. Retain Alpha9.20 Recorder hygiene and all prior Alpha9 behaviour; no optimiser allocation, tariff, Happy Hour, Power Down, FoxESS command/write, Pi Web, Public Web or panel firmware changes; Pi Web remains 0.9.0-alpha9-web.0, Public Web remains 0.9.0-alpha9-public.0, Panel remains 0.9.0-alpha9-panel.0; configured KH7 limits remain 7.0 kW; FoxESS hardware writes remain hard-blocked until physical commissioning is complete",
    "expected_downtime_minutes": 5,
    "home_assistant_restart_required": true,
    "reboot_required": false,
    "affected_components": [
      "kems_core",
      "dashboard",
      "panel",
      "property_web",
      "pi_agent",
      "public_web"
    ]
  }
}
