{
  "schema": 1,
  "bundle": "__RELEASE_VERSION__",
  "channel": "alpha",
  "components": {
    "kems_core": {
      "version": "__RELEASE_VERSION__",
      "required": true,
      "delivery": "home-assistant-update",
      "restart": "home_assistant"
    },
    "dashboard": {
      "version": "__RELEASE_VERSION__",
      "required": true,
      "delivery": "kems_core"
    },
    "panel": {
      "version": "0.9.0-alpha9-panel.0",
      "required": false,
      "delivery": "kems_core"
    },
    "property_web": {
      "version": "0.9.0-alpha9-web.0",
      "required": true,
      "delivery": "kems_pi"
    },
    "pi_agent": {
      "version": "0.9.0-alpha9-web.0",
      "required": true,
      "delivery": "kems_pi"
    },
    "pi_system": {
      "version": null,
      "required": false,
      "delivery": "kems_pi"
    },
    "public_web": {
      "version": "0.9.0-alpha9-public.0",
      "required": false,
      "delivery": "ionos-sftp"
    }
  },
  "maintenance": {
    "required": true,
    "reason": "Alpha9 coordinated parity baseline; Alpha9.12 KEMS-only Agile replay authority repair after Alpha9.11 live proof: adapt detached Snapshot copies at the actual Agile-day simulation boundary so the existing read-only optimiser consumes the same canonical fresh Octopus demand and proposal-solar evidence as the healthy core simulation instead of rejecting every interval because unrelated physical FoxESS battery/SOC/solar/export fields are stale; leave original physical Snapshots and stale flags untouched for commissioning, shadow and control; restore canonical slot energy/SOC and downstream rolling/future flow publication where valid evidence exists; unsupported history remains fail-closed and is not fabricated; no optimiser policy, tariff, Happy Hour, FoxESS command/write, Pi Web, Public Web or panel firmware changes; Pi Web remains 0.9.0-alpha9-web.0, Public Web remains 0.9.0-alpha9-public.0, Panel remains 0.9.0-alpha9-panel.0; canonical 15% optimisation target / 10% hard floor / 12% recovery remains unchanged; FoxESS hardware writes hard-blocked until physical commissioning is complete",
    "expected_downtime_minutes": 5,
    "home_assistant_restart_required": true,
    "reboot_required": false,
    "affected_components": [
      "kems_core",
      "dashboard",
      "panel",
      "property_web",
      "pi_agent",
      "public_web"
    ]
  }
}
