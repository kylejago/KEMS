{
  "schema": 1,
  "bundle": "__RELEASE_VERSION__",
  "channel": "alpha",
  "components": {
    "kems_core": {
      "version": "__RELEASE_VERSION__",
      "required": true,
      "delivery": "home-assistant-update",
      "restart": "home_assistant"
    },
    "dashboard": {
      "version": "__RELEASE_VERSION__",
      "required": true,
      "delivery": "kems_core"
    },
    "panel": {
      "version": "0.9.0-alpha9-panel.0",
      "required": false,
      "delivery": "kems_core"
    },
    "property_web": {
      "version": "0.9.0-alpha9-web.0",
      "required": true,
      "delivery": "kems_pi"
    },
    "pi_agent": {
      "version": "0.9.0-alpha9-web.0",
      "required": true,
      "delivery": "kems_pi"
    },
    "pi_system": {
      "version": null,
      "required": false,
      "delivery": "kems_pi"
    },
    "public_web": {
      "version": "0.9.0-alpha9-public.0",
      "required": false,
      "delivery": "ionos-sftp"
    }
  },
  "maintenance": {
    "required": true,
    "reason": "Alpha9 coordinated parity baseline; Alpha9.23 KEMS-only future-flow export-floor correction after live Alpha9.22 evidence showed the customer-facing 22:30 projection could publish deliberate battery export while estimated SOC fell below the declared 15% planning target, even though house service alone is the only discharge allowed to bridge from 15% toward the independent 10% hard floor. Preserve the original 15% planning/export target, keep ordinary battery-to-home service available down to the 10% absolute floor, and retain the restart-safe 12% recovery latch. The presentation wrapper may lower the effective config reserve only for the final 10% SOC clamp and house-service bridge; it must pin the effective forecast/pre-cheap minimum to the original planning target before invoking the canonical projection so deliberate export cannot inherit the 10% floor. Add executable regression coverage reproducing the live failure with a lower incoming forecast floor and proving export is curtailed at 15% while Home service and zero expensive Grid import are preserved. Retain the Alpha9.22 startup hotfix that keeps build_slot_flow strict, filters policy metadata out of its keyword contract, and prevents the unexpected planning_target_soc_percent keyword failure. Retain Alpha9.13 forecast-aware scheduling, Alpha9.19 runtime Recorder hygiene, Alpha9.20 ShadowValidationRecorder Recorder hygiene and all prior Alpha9 behaviour; no optimiser allocation, tariff, Happy Hour, Power Down, FoxESS command/write, Pi Web, Public Web or panel firmware changes; Pi Web remains 0.9.0-alpha9-web.0, Public Web remains 0.9.0-alpha9-public.0, Panel remains 0.9.0-alpha9-panel.0; configured KH7 limits remain 7.0 kW; FoxESS hardware writes hard-blocked until physical commissioning is complete",
    "expected_downtime_minutes": 5,
    "home_assistant_restart_required": true,
    "reboot_required": false,
    "affected_components": [
      "kems_core",
      "dashboard",
      "panel",
      "property_web",
      "pi_agent",
      "public_web"
    ]
  }
}
