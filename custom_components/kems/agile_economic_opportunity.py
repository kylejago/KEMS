"""Canonical Agile economic-opportunity compatibility boundary.

This Alpha8 facade owns the proven Alpha7.40 economic opportunity guard and its
Agile-first dashboard presentation without rewriting either runtime. Installation
order remains planning guard first, dashboard second, and real FoxESS hardware
writes remain blocked by the existing commissioning boundary.
"""

from __future__ import annotations

from . import agile_economic_opportunity_runtime as opportunity_runtime
from . import dashboard_economic_opportunity_runtime as dashboard_runtime


def install_economic_opportunity() -> None:
    """Install the proven Alpha7.40 planning guard and dashboard in order."""
    opportunity_runtime.install_alpha740_opportunity_guard_patch()
    dashboard_runtime.install_alpha740_agile_primary_dashboard_patch()
