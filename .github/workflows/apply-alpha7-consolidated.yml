name: Apply Alpha7 consolidated patch

on:
  push:
    branches: [feature/alpha7-consolidated-improvements]

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - run: echo "temporary patch runner is available"
