name: Alpha9.45 release identity bootstrap

on:
  push:
    branches:
      - fix/alpha9.45-happy-hour-normalisation

permissions:
  contents: write

jobs:
  bootstrap:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/alpha9.45-happy-hour-normalisation
          fetch-depth: 0
      - name: Apply Alpha9.45 release identity
        run: python .github/alpha945_release_identity.py
      - name: Remove one-shot release bootstrap
        run: |
          rm .github/alpha945_release_identity.py
          rm .github/workflows/alpha945-release-identity.yml
      - name: Commit Alpha9.45 release candidate
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git diff --cached --check
          git commit -m "Alpha9.45: prepare release candidate"
          git push origin HEAD:fix/alpha9.45-happy-hour-normalisation
