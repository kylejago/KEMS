name: Alpha9.4 simulation commissioning migration

on:
  push:
    branches:
      - fix/alpha9.4-simulation-commissioning-separation
    paths:
      - .github/workflows/alpha94-simulation-commissioning-migration.yml

permissions:
  contents: write

jobs:
  migrate-and-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/alpha9.4-simulation-commissioning-separation
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install development tools
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements-dev.txt

      - name: Apply Alpha9.4 migration
        run: python tools/alpha94_migrate.py

      - name: Format candidate
        run: python -m black custom_components/kems/kems_core/simulation.py custom_components/kems/kems_core/scenario_comparison.py custom_components/kems/foxess_command_shadow.py tests/test_alpha94_simulation_commissioning_separation.py tests/test_alpha9_baseline.py tests/test_alpha8_consolidation.py

      - name: Packaged dashboard is current
        run: python scripts/sync_packaged_dashboard.py --check

      - name: Black
        run: python -m black --check --diff .

      - name: Ruff
        run: python -m ruff check .

      - name: Pytest
        run: python -m pytest

      - name: Compile Python source
        run: python -m compileall -q custom_components tests

      - name: Prove FoxESS write boundary remains closed
        run: |
          ! grep -q 'services.async_call' custom_components/kems/foxess_command_shadow.py
          ! grep -q 'write_register' custom_components/kems/foxess_command_shadow.py
          ! grep -q 'async_set_native_value' custom_components/kems/foxess_command_shadow.py

      - name: Commit gated product candidate
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git status --short
          git diff --cached --quiet && { echo 'No candidate changes produced'; exit 1; }
          git commit -m "Alpha9.4: separate simulation from FoxESS commissioning"
          git push origin HEAD:fix/alpha9.4-simulation-commissioning-separation
